{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build & watch",
            "type": "shell",
            "command": "ng b --watch --extract-css"
        },
        {
            "label": "build production",
            "type": "shell",
            "command": "ng b --build-optimizer --prod --extract-css"
        }
    ]
}